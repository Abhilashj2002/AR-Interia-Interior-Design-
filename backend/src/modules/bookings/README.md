# Bookings Module

This module owns bookings domain database access wrappers.

- Tier: backend module (domain)
- DB access: backend/src/db/repositories/bookingsRepository.js
- Runtime: legacy HTTP routes still served from server/index.js during migration
